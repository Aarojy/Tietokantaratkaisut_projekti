spring:
  application:
    name: database_solutions_project
  datasource:
    url: jdbc:mariadb://localhost:3306/tietokantaratkaisutpopulated
    username: db_user
    password:
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8082